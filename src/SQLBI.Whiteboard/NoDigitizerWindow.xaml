<Window x:Class="SQLBI.Whiteboard.NoDigitizerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="No pen or touchscreen found"
        Width="480"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource ToolbarBackgroundBrush}"
        PreviewKeyDown="Window_PreviewKeyDown">
    <StackPanel Margin="28,24">
        <TextBlock FontFamily="Segoe UI"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Foreground="#FF1F2937"
                   TextWrapping="Wrap"
                   Text="No pen or touchscreen found" />
        <TextBlock Margin="0,12,0,0"
                   FontFamily="Segoe UI"
                   FontSize="13"
                   Foreground="#FF374151"
                   TextWrapping="Wrap"
                   Text="SQLBI Whiteboard is built for drawing with a pen or a finger. Windows reports no pen tablet and no touchscreen on this computer." />
        <TextBlock x:Name="ModeText"
                   Margin="0,10,0,0"
                   FontFamily="Segoe UI"
                   FontSize="13"
                   Foreground="#FF374151"
                   TextWrapping="Wrap" />
        <TextBlock x:Name="LimitsText"
                   Margin="0,10,0,0"
                   FontFamily="Segoe UI"
                   FontSize="13"
                   Foreground="#FF374151"
                   TextWrapping="Wrap"
                   Text="A mouse reports no pressure, so a line is drawn at an even width — Calligraphy is the one tool that still varies, because its width comes from speed. A pen would add pressure, the rear eraser, palm rejection, and the hover indicator that shows what a tap would do." />
        <CheckBox x:Name="DoNotShowAgainBox"
                  Margin="0,20,0,0"
                  FontFamily="Segoe UI"
                  FontSize="13"
                  Foreground="#FF374151"
                  Content="Do not show this again" />
        <StackPanel Margin="0,16,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button MinWidth="96"
                    MinHeight="30"
                    IsDefault="True"
                    IsCancel="True"
                    Content="Continue"
                    Click="ContinueButton_Click" />
        </StackPanel>
    </StackPanel>
</Window>
